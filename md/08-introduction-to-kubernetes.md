# Лекция 8. Введение в Kubernetes для бизнеса и аналитики

**Kubernetes (K8s)** — это промышленный стандарт для оркестрации контейнеров. Это не просто "программа для запуска", а платформа, содержащая набор независимых процессов управления, которые **постоянно приводят текущее состояние системы к желаемому**.

### 🎯 Основная концепция: Декларативный подход
В бизнесе мы привыкли ставить KPI. "К концу года выручка должна быть X". Мы не всегда диктуем каждый микро-шаг, как этого достичь.
Kubernetes работает так же:
1.  Вы **декларируете** (описываете в YAML). "Мне нужно 3 копии сайта и 50GB диска".
2.  Вы **не описываете** алгоритм (как скачать, как запустить, как подключить диск).
3.  **K8s делает это за вас**. Ему неважно, как добраться из точки А в точку Б, он просто гарантирует результат.

---

### 💼 Зачем это бизнесу? (Business Value)
Цель Kubernetes — автоматизация развёртывания, масштабирования и управления приложениями в условиях кластера (группы серверов, работающих как единый супер-компьютер).

Это дает компании конкурентные преимущества:
1.  **Time-to-Market**. Ускорение доставки новых фич клиентам.
2.  **Zero Downtime**. Обновление приложений без остановки обслуживания клиентов (Rolling Updates).
3.  **Экономия ресурсов (Cost Efficiency)**. "Уплотнение" приложений. K8s эффективно распределяет задачи по серверам, не давая им простаивать (Bin Packing).
4.  **Самовосстановление (Self-healing)**. Если сервис "упал", K8s перезапустит его мгновенно. Если сервер сгорел, K8s перенесет работу на другой.
5.  **Масштабируемость**. Автоматическая реакция на "Черную пятницу" (HPA) — добавление мощностей под нагрузкой и их отключение, когда нагрузка спадает.

---

### 🏗 Архитектура кластера
Kubernetes кластер — это объединение **Worker Nodes** (где работают приложения) и **Master Nodes** (где принимаются решения).

<details>
  <summary>🧠 Control Plane (Master Node - Управляющий слой)</summary>

Это "мозг" кластера. Здесь принимаются глобальные решения (планирование, реагирование на сбои).

- **`kube-apiserver`**. "Фронт-офис". Единая точка входа. Все команды (от администраторов или скриптов) идут только через него.
- **`etcd`**. "База данных правды". Надежное хранилище ключ-значение. Здесь лежит вся информация о том, каким кластер *должен быть*. Если данные пропадут отсюда — кластер потерян.
- **`kube-scheduler`**. "Логист". Решает, на какой конкретно сервер (Node) отправить задачу (Pod), основываясь на ресурсах (CPU/RAM) и ограничениях.
- **`kube-controller-manager`**. "ОТК / Надзор". Следит за состоянием. Если node упала — он заметит. Если реплик стало меньше — он закажет создание новых.

</details>

<details>
  <summary>💪 Worker Node (Исполнительный слой)</summary>

Это "мускулы" кластера. Здесь крутится полезная нагрузка (ваши сайты, базы данных, скрипты).

- **`kubelet`**. "Прораб". Агент на каждом узле. Получает задачи от "центра" и следит, чтобы контейнеры были запущены и здоровы.
- **`kube-proxy`**. "Сетевой администратор". Настраивает сетевые правила (iptables/IPVS), чтобы трафик попадал к нужным контейнерам.
- **`Container Runtime`**. Среда выполнения. Программа, которая фактически запускает контейнеры.
  > **Важно для современности.** Kubernetes больше не использует Docker напрямую (deprecated). Используются стандартизированные CRI-совместимые среды: **containerd**, **CRI-O**. Для разработчика ничего не меняется — образы Docker по-прежнему поддерживаются.

</details>

---

### 📦 Объекты и Управление
Kubernetes использует формат YAML для описания задач (Манифесты).
У большинства объектов есть два главных поля:
*   `spec`: **Чего мы хотим** (Target State).
*   `status`: **Что мы имеем** (Current State).
*Задача K8s — свести разницу между ними к нулю.*

<details>
  <summary>🌐 Namespace (Пространства имен)</summary>

**Бизнес-аналогия.** Это как отделы в офисе или разные проекты в Jira.
Позволяет разделить один физический кластер между разными командами (Dev, QA, Prod) или проектами, чтобы они не мешали друг другу и можно было считать квоты (деньги) на каждый отдел.

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: analytics-dept # Пространство имен отдела аналитики
```
</details>

---

### 🚀 Запуск приложений (Workloads)

> **Метки (`labels`)** — это как теги в CRM. Ключевой механизм группировки. Например: `env: production`, `tier: frontend`.

<details>
  <summary>🔹 Pod (Под)</summary>

Минимальная единица в K8s. Это "обертка" вокруг одного или нескольких контейнеров.
*   У Пода есть свой IP-адрес внутри кластера.
*   Поды смертны. Если он умер, он не воскресает, а создается новый (с новым IP). Поэтому напрямую Поды создают редко.

</details>

<details>
  <summary>🔹 Deployment (Развертывание) — Для Stateless приложений</summary>

Стандарт для веб-серверов, API, микросервисов.
*   Создает `ReplicaSet`, который гарантирует наличие N копий приложения.
*   Позволяет обновлять версии без простоя.

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bi-dashboard
spec:
  replicas: 3 # Хотим 3 экземпляра дашборда
  selector:
    matchLabels:
      app: bi-dashboard
  template:
    metadata:
      labels:
        app: bi-dashboard
    spec:
      containers:
      - name: dashboard
        image: bi-tool:v2.0
        resources:
          requests: # "Гарантированная зарплата" (минимум ресурсов)
            memory: "512Mi"
            cpu: "250m"
          limits: # "Потолок" (чтобы не съел весь сервер)
            memory: "1Gi"
            cpu: "500m"
        livenessProbe: # "Ты жив?" Если нет - перезапуск.
          httpGet: {path: /health, port: 80}
        readinessProbe: # "Готов работать?" Если нет - не слать трафик.
          httpGet: {path: /ready, port: 80}
```
</details>

<details>
  <summary>🔹 StatefulSet — Для Баз Данных и Хранилищ</summary>

Критически важен для аналитики (PostgreSQL, Kafka, ClickHouse, Elastic).
В отличие от Deployment:
1.  У каждого пода уникальное имя (`db-0`, `db-1`) которое сохраняется при перезапуске.
2.  Гарантирует строгий порядок запуска и остановки.
3.  Каждому поду — свой уникальный диск (PVC).

</details>

<details>
  <summary>🔹 CronJob — Для ETL и отчетов</summary>

**Для аналитика.** Идеально подходит для ночных выгрузок данных, тренировки моделей или генерации отчетов.

```yaml
apiVersion: batch/v1
kind: CronJob
metadata:
  name: daily-report
spec:
  schedule: "0 0 * * *" # Каждый день в полночь
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: reporter
            image: python-etl:latest
            command: ["python", "generate_report.py"]
          restartPolicy: OnFailure
```
</details>

<details>
  <summary>🔹 HorizontalPodAutoscaler (HPA) — Авто-масштабирование</summary>

Экономит деньги бизнеса. Если нагрузка (CPU/Memory) растет — добавляет реплики. Если падает (ночь) — убирает лишние.

</details>

---

### 🔌 Сеть и Доступ

<details>
  <summary>🌐 Service (Сервис)</summary>

Стабильный "внутренний номер" для группы подов. Так как поды умирают и меняют IP, Сервис дает единую точку входа.
*   **ClusterIP**. Внутренний доступ (по умолчанию).
*   **LoadBalancer**. Доступ извне (через облачного провайдера).

</details>

<details>
  <summary>🚪 Ingress (Вход)</summary>

"Ресепшн" кластера. Управляет внешним доступом по HTTP/HTTPS. Позволяет на одном IP иметь много сайтов (`site.com/api`, `site.com/admin`).
*В новых версиях K8s постепенно внедряется **Gateway API**, как более мощная замена Ingress, но Ingress всё еще стандарт.*

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: analytics-ingress
spec:
  rules:
  - host: analytics.corp.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bi-dashboard
            port: {number: 80}
```
</details>

---

### 💾 Данные и Конфигурация (12 Factor App)

<details>
  <summary>💿 PersistentVolume (PV/PVC)</summary>

**Бизнес-контекст.** Контейнеры эфемерны (данные внутри них пропадают при перезапуске).
Чтобы сохранить данные (БД, файлы отчетов), мы запрашиваем **PersistentVolumeClaim (PVC)**. Это как "внешний жесткий диск", который Kubernetes сам подключит к нужному серверу, где запустился ваш под.

</details>

<details>
  <summary>📝 ConfigMap и Secret</summary>

Разделение кода и конфигурации.
*   **ConfigMap**. Хранение `config.yaml`, `nginx.conf` или переменных среды. Позволяет менять настройки без пересборки образа приложения.
*   **Secret**. Хранение паролей от БД, API-ключей. Данные закодированы (Base64) и монтируются в контейнер безопасно.

</details>
